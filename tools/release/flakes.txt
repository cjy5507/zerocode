# Known load flakes the release lane judges solo ×3 instead of calling red.
# One test per line, `root:` for the root gate (just verify at the repo root),
# `zo:` for the zo gate (zo-ide/just verify). A red naming anything NOT listed
# here is red for real (design §3.5). Matching is by substring of the failing
# test path in cargo's `failures:` block, so a bare name covers every module.
# History: memory zerocode-repo-tooling-traps 80/93, t-2863 (the sidecar
# contract), the old ~/.local/share/zerocode/release/release-judge.sh.
root:codex_queue::tests::installed_codex_starts_and_cleans_an_explicit_worker_sidecar
# 2026-09-14 (coordinator gate for t-3993 on a loaded machine, load 11-19): the sidecar probe read `Unknown` where
# `Confirmed` was expected in one of three full `--bins` runs, green solo x3 — the same codex-sidecar timing family as
# the line above; the two fake-Codex nudge tests below flaked in the other two runs, also green solo x3.
root:codex_queue::tests::bound_queue_process_receives_the_fixed_pointer_and_minimal_environment
zo:preferences::tests::persisted_fast_models_are_normalized_to_the_bare_family
zo:a_pane_shrunk_without_sigwinch_loses_no_rows
zo:opening_a_pane_uses_its_private_events_channel
# 2026-09-14 v1.3.62 root gate (load 26 while two worker builds ran beside it):
# the same zerocode-lane supervisor test (crates/zerocode-lane/tests/supervisor.rs)
# read PaneChannelNotReady{waited: 2s} — the root workspace runs it too, and the
# zo: prefix above does not cover the root lane. Same timing family; judge solo ×3.
root:opening_a_pane_uses_its_private_events_channel
zo:todo_lifecycle_hooks
zo:a_pane_grown_without_sigwinch
# 2026-09-07 first lane run (3e97e3f9): the zo e2e waits for R34_FOLLOW_UP_DONE and timed out while the root
# and zo gates ran in parallel for the first time; the same name flaked in rel-zo-0bbc7260/2d4ac0e5/310f200a.
# A timing wait under load, not a regression — judged solo x3 like the other pane timings.
# 2026-09-07 second lane run (2f83aedb, gates still side by side): three more deadline waits under load —
# the fake-Codex nudge deadline (restart_nudge_runtime.rs:478) and zo's SIGTERM hand-back within TEST_TIMEOUT
# (e2e_hermetic.rs:2507). Judged solo x3; the lane now runs the gates one after the other (PARALLEL_GATES=0).
root:restart_nudge_runtime::tests::a_codex_resume_that_banners_mcp_warnings_before_its_prompt_still_submits
root:restart_nudge_runtime::tests::a_fake_codex_resume_receives_the_nudge_on_stdin_and_yields_one_log_line
zo:e2e_sigterm_ends_the_session_and_hands_the_terminal_back
# 2026-09-10 local gates run side by side again (root+zo+xwin, load 13–17): the scheduler e2e's ninety-second
# wakeup wait missed its deadline (e2e_hermetic.rs:5946) and passed solo in 53 s. A deadline wait under load.
zo:e2e_schedule_wakeup_fires_once_inside_ninety_seconds
# 2026-09-11 01:54 lane (c8627a49): the machine sat at load 30 with fseventsd at 93% CPU for hours after the
# day's gates and cross-compiles churned ~60 GB of target output; six pty e2e tests timed out "waiting for
# the terminal to settle" (e2e/harness.rs:57) and the gate took 6 h. All six passed solo in 75 s once load
# fell to 2. Terminal-settle waits under load, like the pane timings above.
zo:e2e_a_context_trim_stands_on_the_status_row_and_never_in_the_transcript
zo:e2e_background_exit_reaches_next_boundary_and_persisted_task_output
zo:e2e_capabilities_report_real_build_and_keep_launch_immutable_across_session_changes
zo:e2e_compaction_after_a_tool_turn_died_sends_a_valid_summary_request
zo:e2e_continue_after_a_tool_turn_died_mid_flight_sends_a_valid_request
zo:e2e_continue_replays_two_completed_turns_from_the_same_session

# 2026-09-22 (coordinator gate after the J6 merge, four workers building, load 30+): the fake iOS helper's channel handshake
# timed out ("timed out waiting on channel") once in the full shell-test; green solo x3 in 0.34 s — a load flake of t-5763's corpse test.
root:emulator::ios_hid::tests::a_corpse_is_replaced_before_it_is_handed_out_and_nobody_gets_a_missing_device

# 2026-09-22 07:2x v1.1.14 coordinator gate (a worker's first full build ran beside it; zo-test 06:09–06:16): the tools
# crate's read-only sandbox bash and the python REPL kernel each failed to start once in the full `just test`
# ("No such file or directory", "python runtime not found"); green solo ×3 in 0.05 s and green in the five gates before.
# Process-spawn timing under load — the same family as the pane timings above.
zo:permissions_misc::given_read_only_enforcer_when_read_only_safe_bash_then_allowed
zo:shell::repl_executes_python_code_in_a_persistent_kernel

# 2026-09-22 07:5x zo gate after the crossterm input-source change (a worker's full root workspace build beside it, load 26):
# four runtime spawn tests failed at once — the formatter script never recorded its grandchild pid in 3 attempts, and the
# fake LSP server's initialize/hover round trips ran past their 5 s budget. Green solo ×3 in 1.0–1.3 s at load 49 through
# the same workspace invocation `just test` uses. Process-spawn and 5 s deadline waits under load, like the family above.
zo:auto_format::tests::formatter_timeout_kills_process_group
zo:lsp_client::tests::stdio_transport_bounds_open_document_bookkeeping
zo:lsp_client::tests::stdio_transport_round_trips_real_jsonrpc_requests
zo:lsp_client::tests::sync_and_collect_diagnostics_returns_did_change_diagnostics
# 2026-09-22 (coordinator gate for t-5961, `just shell-test` at load 20-27 beside a release-lane build): the ephemeral
# port the test frees and rebinds was taken by a neighbour between the two binds (`EADDRINUSE` from bind_seat(port)),
# not by our own lingering connection — SO_REUSEADDR covers only the latter. Green solo ×3 in 0.00 s.
root:google_login::tests::the_callback_seat_can_be_retaken_while_the_last_one_lingers
# 2026-09-22 (coordinator gate for t-5966, `just shell-test` at load 60-74 beside four worker builds): the test expects
# its hand-driven `collect_stale` to answer Fresh (the hook already took the pointer), but under load the run outlived
# HOOK_COLLECTION_GRACE, so the parked pointer read as stale instead (tests.rs:9857). Orchestration untouched by the
# branch; green solo ×3 in 0.37–0.86 s. A wall-clock grace measured against a loaded test run, like the families above.
root:orchestration::tests::a_working_claude_pane_is_pointed_at_through_its_own_hook_and_never_its_composer
