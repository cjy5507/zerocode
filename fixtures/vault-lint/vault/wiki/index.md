# Wiki

Fixture index. Two pages are deliberately not listed here: `index-gap` (reached
from the hub by a declared relation) and `orphan` (reached by nothing).

## Topics
- [[hub]] — the page with the ghost link and the undeclared prose link
- [[indexed-clean]] — a clean page
- [[linked-target]] — a clean page the hub links in prose
- [[no-source]] — a page missing its `source:` key
- [[old-note]] — the page the hub supersedes
